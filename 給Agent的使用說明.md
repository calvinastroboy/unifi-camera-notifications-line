# 請你的 Agent 協助設定

這份套件包含給 AI 助手使用的技能：

`skills/unifi-line-kit-assistant/SKILL.md`

它的工作是指揮 Agent 使用已經寫好的腳本，不是再寫一套程式。帳號申請、登入、驗證碼和付款／訂閱同意仍由你處理。

## 最簡單的用法：不用先安裝 Skill

1. 把分享包解壓縮到你的電腦。一個客戶使用一份獨立資料夾。
2. 在能操作本機檔案及終端的 Agent 中，開啟這個 `unifi-line-kit` 資料夾，或告訴它完整路徑。
3. 複製以下這段話給它：

> 請先讀取這個套件內的 skills/unifi-line-kit-assistant/SKILL.md，照技能協助我完成 UniFi 攝影機截圖推送 LINE。優先使用現成腳本，遇到需要的步驟才讀相應文件，不要重新開發。先確認目前進度，已完成的不要重做。API 金鑰由我在本機網頁填寫；需要我註冊、登入或操作手機時，請簡單告訴我。部署、配對、修改 Alarm 和發送測試前，確認我的操作範圍。不要在對話中列出金鑰或帶密鑰的網址。

若你只想排查問題，可以改成：

> 請依 skills/unifi-line-kit-assistant/SKILL.md 排查這個套件的通知問題。先做唯讀檢查，不要重建部署、重設配對或發送測試。告訴我已確認什麼，以及下一個需要我做的動作。

## Codex、Claude、Hermes 怎麼用？

技能使用一般 Markdown 文件，沒有綁定某一家的專屬工具。以上「讓 Agent 讀取檔案」的方式不依賴特定 Skill 安裝位置。

- **Codex 本機 Agent**：把套件資料夾作為專案開啟，再貼上指示。
- **Claude 的本機 Agent（例如 Claude Code）**：在套件資料夾啟動／開啟工作階段，再貼上指示。
- **Hermes**：使用具備本機檔案與終端存取的環境，提供套件路徑，再貼上指示。如果 Hermes 在另一台主機，它不會自動看得到你電腦上的資料夾或網頁。

若你的 Agent 有「匯入技能」功能，可以依該產品當前的說明，匯入整個 `skills/unifi-line-kit-assistant` 資料夾，而不是只匯入 SKILL.md。匯入後仍須提供真正的套件位置；技能本身不包含可獨立部署的全部程式。

只有聊天功能、沒有檔案或終端工具的版本，只能教你操作，不能代你執行腳本。若 Agent 在遠端，請在自己的電腦使用本機網頁填資料，不要把設定網頁暴露到網際網路。

這是跨 Agent 的文字操作技能，不代表已在每個產品與版本上做過實機相容性測試。

## 為什麼比較省 token？

- Agent 先讀精簡的技能入口，不必讀整段歷史對話。
- 部署、檢查、配對與打包都交給現成腳本。
- 正常流程不需要閱讀原始碼或完整操作指南。
- 只在出錯時讀相應排錯表，不反覆從頭搜尋。
- 依已有狀態接續，不重做帳號申請和部署。

不保證固定節省百分比；實際用量取決於 Agent、模型及遇到的問題。雲端服務費用與 LINE 訊息額度也不因用了 Skill 就消失。

## 分享前請注意

只分享 `dist/unifi-line-kit.zip`。裡面包含腳本、網頁、完整操作指南和這份 Agent 技能。

不要分享已設定的工作資料夾，尤其是 `.private`，它可能包含明文金鑰、收件人及部署資料。不要把你的 API key 貼入 Agent 對話以求省事。

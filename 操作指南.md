# UniFi → LINE 圖片通知：完整操作指南

適用：本機網頁設定精靈 1.2 版｜更新日期：2026-09-05

這份指南是給不熟悉技術的使用者看的。你不用寫程式，照順序準備帳號、貼上資料、按畫面上的按鈕即可。

第三方網站可能更新選單名稱；以下同時列出中英文名稱。若找不到同名選單，先確認登入的是正確帳戶與產品，不要隨意更改其他設定。

## 先看懂：這套工具會做什麼？

例如有人進入展廳時：

**UniFi 偵測到事件 → Cloudflare 等待指定秒數並取得相機畫面 → 圖片暫存到 R2 → LINE 收到文字與圖片。**

- 可以用門磁開門觸發，也可以用相機支援的人員偵測等事件觸發。
- 每次可以傳送 1–4 台相機的截圖。
- 完成部署後，設定用的電腦可以關閉，不需要在客戶現場放一台常開電腦。
- 現場的 UniFi 主機、相機和網路仍然必須正常運作。
- 這版不需要 Home Assistant、IFTTT，也不需要經過 Email 轉寄。

這是輔助通知工具，不是保證每次必達的保全系統。服務中斷、額度不足或網路問題，都可能造成通知失敗。

## 一、開始前的準備清單

### 1. 你需要的設備

- 一台可以上網的 Mac 或 Windows 電腦，用來執行設定工具。
- 一台已經安裝 LINE 的手機。
- 已設定好的 UniFi Protect 主機與相機。
- 如果要用開門觸發，需要 UniFi 能辨識狀態的門磁感測器。
- 如果不用門磁，攝影機必須支援你要用的偵測事件。

先用 UniFi 網頁或 App 確認看得到相機的即時畫面。看不到畫面時，先修復相機或網路，再設定通知。

### 2. 你需要的帳號與資料

| 服務 | 需要準備什麼 | 等一下貼到哪裡 |
|---|---|---|
| Cloudflare | Account ID／帳戶 ID | 連接服務 → Cloudflare → 帳戶 ID |
| Cloudflare | API token／部署權杖 | 連接服務 → Cloudflare → 部署權杖 |
| UniFi | Console ID／主機 ID | 連接服務 → UniFi Protect → 主機 ID |
| UniFi | API key | 連接服務 → UniFi Protect → API key |
| LINE | Channel access token | 連接服務 → LINE Messaging API → Channel access token |
| LINE | Channel secret | 連接服務 → LINE Messaging API → Channel secret |

**ID 像地址；API key、token、secret 像鑰匙。不要把地址填進鑰匙欄，也不要用帳號登入密碼代替金鑰。**

金鑰建立後可能只顯示一次。先放進密碼管理器，或直接貼進工具；不要存到公開記事本、聊天群組或截圖教學裡。這份指南也不要填入真實金鑰後再轉發。

### 3. 每個客戶都要分開

- 每個客戶使用一份全新解壓縮的套件。
- 用客戶擁有或明確授權管理的帳戶。
- 不要把客戶 A 已經設定過的整個資料夾複製給客戶 B。
- 如果你只是替同一位客戶修改設定，使用原來的資料夾，不要從零重新建立。

## 二、準備 Cloudflare 帳號

Cloudflare 負責在雲端執行通知程式，R2 負責暫存圖片。

### 2-1. 註冊或登入

1. 開啟 [Cloudflare 管理後台](https://dash.cloudflare.com/)。
2. 有帳號就登入；沒有帳號就選註冊，依畫面完成信箱驗證。
3. 如果可以選擇多個帳戶，進入這次客戶要使用的帳戶。
4. 若出現登入驗證、驗證碼或條款，請由帳戶持有人自行完成。

本套件使用 Cloudflare 提供的 workers.dev 網址，不需要為了這次設定購買網域或把客戶的網站搬到 Cloudflare。

### 2-2. 開通 R2 圖片儲存服務

1. 在 Cloudflare 後台找到 **R2／R2 object storage**。
2. 第一次使用時，依畫面完成啟用流程。
3. 若要求帳單資料或同意計費條款，請客戶先閱讀並自行決定。
4. 能進入 R2 儲存空間的管理頁，即可繼續。

目前這套工具會自動建立自己需要的圖片儲存空間。**不用先手動建立 bucket，也不用另外申請 R2 的 S3 Access Key。**

不要把「免費額度」理解成永遠不會收費；用量和方案以帳戶後台為準。參考：[R2 官方入門](https://developers.cloudflare.com/r2/get-started/)。

### 2-3. 確認 workers.dev 網址

1. 在 Cloudflare 找到 **Workers & Pages**。
2. 查看帳戶的 **Your subdomain／workers.dev 子網域**。
3. 如果首次使用需要設定名稱，依畫面完成。
4. 如果已經有名稱，保留即可，不必為本工具更改。

帳戶子網域的樣子是 `你的名稱.workers.dev`。工具部署後的完整服務網址會再加上服務名稱。

這裡只是準備帳戶的網址，不是手動建立整個通知程式。如果選單和指南不同，參考 [workers.dev 官方說明](https://developers.cloudflare.com/workers/configuration/routing/workers-dev/)；不要靠亂建多個 Worker 解決。

### 2-4. 取得 Account ID

1. 留在正確的 Cloudflare 帳戶。
2. 查看瀏覽器網址。
3. 找到 `dash.cloudflare.com/` 後方的 32 位英數字，複製那一段。
4. 稍後貼入工具的「帳戶 ID」。

示意：`https://dash.cloudflare.com/【這裡是帳戶 ID】/…`

只複製 ID，不包含斜線或後面的頁面路徑。這不是 Zone ID，也不是 API token。

### 2-5. 建立部署用的 API token

1. 開啟 [Cloudflare API Tokens](https://dash.cloudflare.com/profile/api-tokens)。
2. 或從 **My Profile／我的個人資料 → API Tokens** 進入。
3. 按 **Create Token／建立 Token**。
4. 選 **Custom Token／自訂 Token**。
5. 名稱可填「UniFi LINE 通知部署」。
6. 加入下表三項權限。

| 第一欄 | 第二欄 | 第三欄 |
|---|---|---|
| 帳戶／Account | Workers 指令碼／Workers Scripts | 編輯／Edit |
| 帳戶／Account | Workers KV 儲存空間／Workers KV Storage | 編輯／Edit |
| 帳戶／Account | Workers R2 儲存空間／Workers R2 Storage | 編輯／Edit |

7. 在 **Account Resources／帳戶資源** 選「包含」，指定這次客戶的帳戶。
8. 檢查到期時間與 IP 限制是否符合安裝電腦的使用情況；不懂時請管理員協助，不要亂填限制值。
9. 按 **Continue to summary／繼續至摘要**，核對三項權限與帳戶範圍。
10. 按 **Create Token**，複製產生的完整 token。
11. 稍後貼入工具的「部署權杖」。

**最常選錯：Workers 指令碼，不是 Workers 代理程式設定。**

這把 token 是讓安裝電腦部署程式用的；不是 R2 S3 金鑰，也不是 Cloudflare 的 Global API Key。官方流程與只顯示一次的提醒見 [建立 API token](https://developers.cloudflare.com/fundamentals/api/get-started/create-token/)。

**完成標準：你已取得 Account ID 和部署權杖，R2 可進入管理頁，workers.dev 已可用。**

## 三、準備 UniFi 帳號與 API key

### 3-1. 確認帳號能管理現場主機

1. 開啟 [UniFi Site Manager](https://unifi.ui.com/)。
2. 登入客戶的帳號，或客戶已授權給你的管理帳號。
3. 選擇這次要設定的主機。
4. 進入 Protect，確認看得到要使用的相機與即時畫面。
5. 確認主機可經 Site Manager 遠端連線。

如果你看不到建立 API key 的選項，請帳戶擁有者檢查權限。不要用其他客戶的 key 代替，也不要為此重設主機。

### 3-2. 建立 API key

1. 回到 UniFi Site Manager 的設定。
2. 尋找 **Settings → API Keys／設定 → API 金鑰**。
3. 選 **Create New API Key／建立新的 API 金鑰**。
4. 若需要名稱，可填「LINE 圖片通知」。
5. 若提供組織或權限範圍選項，確認涵蓋這次主機及 Protect 所需存取，不多開不必要權限。
6. 建立後複製完整 key，安全保存。
7. 稍後貼入工具的「UniFi API key」。

請使用 Site Manager 的 API key。本工具經 UniFi 雲端取得畫面，不是直接連到現場的內網 IP。金鑰建立入口及一次性顯示的說明見 [UniFi Protect 官方 API 入門](https://developer.ui.com/protect)。

### 3-3. 取得 Console ID

1. 從 Site Manager 打開這台主機的 Protect。
2. 查看瀏覽器網址。
3. 找到 `/consoles/`。
4. 複製其後、下一個 `/` 之前的完整那一段。
5. 若 ID 裡包含冒號及後面的數字，一併保留。
6. 稍後貼入工具的「主機 ID」。

不要填：主機名稱、內網 IP、相機 ID，或只截取 ID 的前半段。如果這個版本的網址不顯示 `/consoles/`，請管理員協助核對，不要猜。

攝影機 ID 暫時不用找，之後可以在工具內按「讀取攝影機」。

**完成標準：你能看相機畫面，並取得這台主機的 Console ID 與可用 API key。**

## 四、準備 LINE 官方帳號與 API

手機上的個人 LINE 是「收通知的人」；LINE 官方帳號是「發通知的機器人」。兩者不是同一個東西。

### 4-1. 建立或選擇專用官方帳號

1. 開啟 [LINE Official Account Manager](https://manager.line.biz/)。
2. 使用客戶擁有或授權的帳號登入。
3. 新增官方帳號，或選擇要專門用來發通知的既有帳號。
4. 名稱可用「店名／展廳通知」，讓收件人容易辨識。
5. 依頁面填寫資料、確認條款並完成建立。

如果原本帳號已經接客服、訂單或其他機器人，先不要改它。這套工具需要使用該 channel 的 Webhook，可能與既有系統衝突。

### 4-2. 啟用 Messaging API

1. 在 Official Account Manager 選取該官方帳號。
2. 進入設定，找到 **Messaging API**。
3. 選擇啟用 Messaging API。
4. 如果要求開發者姓名與信箱，依實際資料完成。
5. 選擇或建立 **Provider**。可以把它理解成「這個機器人歸屬哪家公司／管理者」。
6. 確認選的是客戶正確的 Provider，再完成啟用。

Provider 綁定後不能任意改派，請不要把互不相關客戶都放到不適合交接的個人 Provider。現在建立 Messaging API channel 的入口是先啟用官方帳號的 Messaging API，不是直接在 Developers Console 新建。參考 [LINE 官方入門流程](https://developers.line.biz/en/docs/messaging-api/getting-started/)。

### 4-3. 取得 Channel access token

1. 開啟 [LINE Developers Console](https://developers.line.biz/console/)。
2. 用剛才同一個管理帳號登入。
3. 選擇剛才的 Provider。
4. 選擇對應的 **Messaging API channel**，不要選成 LINE Login。
5. 進入 **Messaging API** 分頁。
6. 找到 **Channel access token (long-lived)**。
7. 若尚未建立，按 **Issue／發行**。
8. 複製完整 token，稍後貼入工具同名欄位。

本套件不會自動續期 token，因此這份一般使用者流程採用 long-lived token。不要填短期測試 token；也不要沒事重按 Reissue，重新發行會影響舊 token 的效力。參考 [LINE token 類型及重新發行說明](https://developers.line.biz/en/docs/basics/channel-access-token/)。

### 4-4. 取得 Channel secret

1. 留在同一個 Messaging API channel。
2. 切換到 **Basic settings／基本設定**。
3. 找到 **Channel secret**，複製。
4. 稍後貼入工具的「Channel secret」。

注意：Channel ID、Channel secret、Channel access token 是不同資料。這裡需要的是後兩個。

### 4-5. 把機器人加入好友

1. 在 Messaging API 分頁找該官方帳號的 QR code，或使用官方帳號提供的加好友連結。
2. 用將來要收通知的手機 LINE 掃描。
3. 加入好友，確認沒有封鎖該官方帳號。

如果收到預設歡迎訊息，只代表加好友成功，不代表本工具已完成配對。若專用帳號的自動回覆造成混淆，可在 Official Account Manager 關閉不需要的歡迎或自動回覆；不要誤停其他業務需要的回覆。參考 [LINE 官方 Bot 設定說明](https://developers.line.biz/en/docs/messaging-api/building-bot/)。

**完成標準：已取得同一個 channel 的 token 和 secret，而且收件人的手機已加入該機器人好友。**

## 五、安裝並開啟設定工具

### 5-1. 安裝 Node.js

Node.js 是讓這套工具能在電腦上執行的軟體。只需安裝，不需要學寫程式。

1. 開啟 [Node.js 官方下載頁](https://nodejs.org/en/download)。
2. 選擇適合電腦的安裝程式，版本需為 22 或更新。
3. 依安裝精靈完成安裝。
4. 若工具仍說找不到 Node.js，關閉原本終端，再重新開啟啟動檔。

### 5-2. 解壓縮套件

1. 下載安裝人員提供的 `unifi-line-kit.zip`。
2. 完整解壓縮，不要直接在 ZIP 裡執行檔案。
3. 將資料夾放在你有權限且容易找到的位置。
4. 記住這是「這位客戶的設定資料夾」，之後修改請開同一份。

不要放在公開共享資料夾。使用中會儲存客戶金鑰，請避免不必要的雲端同步。

### 5-3. 開啟網頁精靈

- Mac：雙擊 `Start.command`。
- Windows：雙擊 `Start.cmd`。Windows 啟動檔目前尚未經 Windows 實機驗證。

第一次啟動會安裝必要套件，請保持網路連線並等待。接著瀏覽器會開啟「連線小幫手」。

你可能同時看到一個終端視窗和一個網頁。**設定期間請保留終端視窗，不要把它關掉。**

若瀏覽器沒自動打開，複製終端顯示的完整本機網址開啟。網址開頭通常是 `http://127.0.0.1:`；後面的連接埠可能每次不同，不要照抄別人的數字。完整啟動網址含本機連線憑證，也不要分享。

若系統阻擋檔案或提示權限錯誤，先確認套件來源並請安裝人員協助，不要停用電腦的安全防護。

## 六、網頁第 1 步：準備帳號

1. 檢查 UniFi、Cloudflare、LINE 都已完成上述準備。
2. 需要補資料時，可用頁面中的官方連結開啟後台。
3. 按 **開始設定 →**。

已經有設定的使用者不需要重新申請所有資料。頁面上「設定已儲存」只表示本機有資料，不表示通知已經成功。

## 七、網頁第 2 步：連接服務

照本指南第一張表，依序填入六個欄位：

1. Cloudflare 帳戶 ID。
2. Cloudflare 部署權杖。
3. UniFi 主機 ID。
4. UniFi API key。
5. LINE Channel access token。
6. LINE Channel secret。

填寫時注意：

- ID 欄只填 ID，不貼整個網址。
- 金鑰欄位被遮住是正常的，不是沒有貼成功。
- 不要把換行、空白或 `Bearer` 字樣一起貼入金鑰欄。
- 已有金鑰會顯示「已設定」，留空表示保留，不是刪除。
- 欄位錯誤時依訊息修正；其他已填資料不用全部重來。

填好後按 **儲存並選擇相機 →**。

**完成標準：出現「資料已儲存」，並進入選擇攝影機頁。此時只儲存到電腦，還沒有部署或發送通知。**

若帳戶 ID 在第一次填寫時有誤，尚未開始建立部署前可以修正。已開始部署後，帳戶可能會鎖定，以免把現有資源誤接到另一位客戶。

## 八、網頁第 3 步：選擇攝影機

### 8-1. 讀取與勾選

1. 按 **讀取攝影機**。
2. 等待清單出現相機名稱。
3. 勾選要收到的相機，最少 1 台、最多 4 台。
4. 使用 **↑／↓** 調整圖片排列順序。

例如展廳可以選「入口相機」在前、「室內相機」在後。這是兩張獨立圖片，不是合成一張。

如果重新開啟工具後先看到 ID，而不是熟悉的相機名稱，按「讀取攝影機」即可重新載入名稱。

### 8-2. 清單讀不到時

先確認 UniFi 主機 ID、API key 與權限。不同 Protect 版本也可能影響清單讀取。

若安裝人員已提供正確相機 ID：

1. 展開 **無法讀取清單？手動輸入攝影機 ID**。
2. 貼入 ID；多台用英文逗號分隔。
3. 按 **使用這些相機**。
4. 再按本頁最後的 **儲存並繼續 →**，才算儲存。

請不要把 MAC 位址、相機名稱或主機 ID 當作相機 ID 猜填。

### 8-3. 設定通知文字與時間

1. 在「通知文字」填入要顯示在 LINE 的內容，例如「🚪 展廳有人來了」。
2. 用滑桿選擇等待 0–10 秒後截圖，預設 5 秒。
3. 按 **儲存並繼續 →**。

延遲從雲端收到並處理事件後計算，還要加上網路和抓圖時間，不是保證 LINE 恰好在第 5 秒收到。

如果人物走得很快，固定等 5 秒不一定更清楚；可縮短時間，或選更適合拍到進出動線的相機。這版抓的是當下畫面，不會倒回錄影挑最好的一幀。

**完成標準：下一頁顯示的相機數量與截圖延遲正確。之後修改文字、相機或延遲，都需要重新部署才會在雲端生效。**

## 九、網頁第 4 步：檢查與部署

### 9-1. 先檢查連線

1. 核對帳戶 ID、服務名稱、相機數量與延遲。
2. 按 **先檢查連線**。
3. 等待進度區顯示完成。

這一步會檢查 Cloudflare、LINE 機器人資料與相機圖片能否讀取。它會讀取畫面作檢查，但不會發送 LINE 通知。

如果失敗，先看錯誤訊息，修正對應資料再檢查。讀取檢查通過，仍不代表部署所需寫入權限一定正確。

### 9-2. 部署至 Cloudflare

1. 確認沒有選錯客戶。
2. 按 **部署至 Cloudflare ↗**。
3. 等待工具完成建立或更新雲端服務、儲存空間和金鑰。
4. 看到 **部署已完成。下一步：連接 LINE。** 後再繼續。

不用自己手動建立 Worker、KV 或圖片 bucket，也不用把程式碼貼到 Cloudflare。

部署過程中不要關閉終端、讓電腦睡眠或一直連按按鈕。若看到「已部署但尚未就緒」，先等一下再檢查狀態，不要立刻刪除資源、重新申請帳號或重填全部設定。

**完成標準：部署工作顯示完成。這只表示程式已放上雲端，還要完成 LINE 配對和實際通知測試。**

## 十、網頁第 5 步：連接 LINE

這一步告訴系統「這個機器人的通知要送給哪個人」。本版配對流程以個人私訊為主，不是群組配對精靈。

### 10-1. 取得配對資料

1. 按 **開始配對**。
2. 等待畫面顯示 LINE Webhook URL 與配對碼。
3. 保持頁面開啟。

如果直接顯示「LINE 已連接」，表示已有收件人，不必重新配對。要更換收件人時，請安裝人員協助；本版不提供直接換綁按鈕。

### 10-2. 將 LINE Webhook 貼到 LINE 後台

1. 在工具中複製 **LINE Webhook URL**。
2. 開啟 LINE Developers，選擇相同 Provider 和 Messaging API channel。
3. 進入 **Messaging API → Webhook settings**。
4. 編輯 **Webhook URL**，貼上工具提供的網址並儲存。
5. 按 **Verify／驗證**，確認顯示 Success。
6. 確認 **Use webhook／使用 Webhook** 已開啟。

網址末尾應為 `/line`，不是 `/unifi-alarm`，也不是電腦的 `127.0.0.1` 網址。一個 channel 只能設定一個 Webhook 目的地；既有商用 channel 不可未確認就覆蓋。參考 [LINE Webhook 官方設定](https://developers.line.biz/en/docs/messaging-api/building-bot/)。

### 10-3. 傳送配對碼

1. 在工具中複製完整配對碼，包含 `PAIR-` 開頭。
2. 用要收通知的個人 LINE，開啟與該機器人的私人聊天。
3. 傳送整段配對碼，不要多加解釋文字。
4. 回到工具等候更新；系統約每 10 秒檢查一次。
5. 看到 **✓ LINE 已連接，可以到下一步發送測試。** 才算成功。

配對碼 15 分鐘有效。到期就重新按「開始配對」取得新碼。不要一直重按，否則容易把新舊配對碼弄混。

注意：本工具以網頁狀態顯示配對成功，不保證機器人會在 LINE 裡回覆「配對成功」。LINE 後台的 Verify 成功，也只代表 Webhook 可接收，不代表收件人已經綁好。

**完成標準：本工具顯示「LINE 已連接」。**

## 十一、網頁第 6 步：發送測試

1. 按 **發送測試 ↗**。
2. 等待執行結果。
3. 打開手機 LINE，確認收到通知。
4. 逐張檢查圖片來自正確相機、可以顯示，而且時間接近剛才測試。
5. 確認圖片順序正確。
6. 完成後勾選「我已在手機確認：文字和每一張圖片都有收到」。

畫面顯示「LINE API 已接受」的意思是 LINE 收下了發送請求，**不等於你手機上的圖片一定顯示正常**。

如果提示「LINE 已接受，但圖片檢查失敗」，先看手機，不要立即一直重送。再次按測試可能再產生一則通知，也會使用訊息額度。

**完成標準：不是只有網頁綠勾，而是手機真的看到正確文字與每一張圖片。**

## 十二、設定 UniFi 自動觸發

完成上一節只代表「手動測試可以送出」。要自動運作，還要在 UniFi 建立或修改 Alarm 規則。

### 12-1. 先分清楚兩個 Webhook

| 名稱 | 網址末尾的樣子 | 要貼到哪裡 |
|---|---|---|
| LINE Webhook | `/line` | LINE Developers → Messaging API → Webhook URL |
| UniFi Webhook | `/unifi-alarm?token=…` | UniFi Protect → Alarm → Webhook 動作 |

兩者不能交換使用。UniFi 的完整網址內含驗證金鑰，不要分享截圖，也不要刪掉 `?token=` 後面的內容。

### 12-2. 取得 UniFi Webhook

1. 回到工具第 6 步。
2. 在「最後，讓 UniFi 觸發通知」按 **取得 Webhook**。
3. 按 **複製**，複製完整 UniFi Webhook URL。

### 12-3. 在 UniFi 建立 Alarm

1. 開啟 UniFi Protect。
2. 進入 **Alarm Manager／警報管理**。
3. 新增一條規則，或編輯這次要使用的既有規則。
4. 填寫容易辨識的名稱，例如「展廳開門 LINE 通知」。
5. 選擇觸發事件、來源裝置及需要的時段。
6. 在 **Action／動作** 新增 **Webhook／Custom Webhook**。
7. 貼上工具複製的完整 UniFi Webhook URL。
8. 在方法或進階設定中選 **HTTP POST**。
9. 儲存規則，確認已啟用。
10. 再重新打開規則，核對來源、事件、POST、網址和時段都仍正確。

**不要忽略 POST。** UniFi 官方說明指出預設可能是 GET，而本工具的 Alarm 接收端需要 POST。參考 [UniFi 官方 Webhook 設定](https://help.ui.com/hc/en-us/articles/25478744592023-Send-UniFi-Protect-Alerts-to-Web-Services-using-Webhooks)。

### 12-4. 有門磁時怎麼選？

- 選感測器／Sensors 類型。
- 指定真正安裝在這扇門上的門磁。
- 選擇代表「開啟」的狀態事件，部分版本顯示 Open Status Changed。
- 若只要開門通知，不要再加上「同時偵測到人」的 AND 條件。

門磁本身不會拍照，但可以觸發工具去取你指定攝影機的畫面，不需要靠門磁事件附帶縮圖。

### 12-5. 沒有門磁時怎麼選？

依攝影機與 Protect 版本支援的功能，可選：

- **Person／人員偵測**：有人被辨識時通知。
- **Motion／動作偵測**：畫面有動作時通知，觸發可能較頻繁。
- **越線偵測**：有人越過指定線條時通知；需裝置及版本支援。

先在 UniFi 把偵測範圍、線條或條件設好，再指定它作為 Alarm 來源。

觸發事件的裝置與截圖相機可以不同。例如由入口的偵測事件觸發，同時傳入口和室內兩台相機。**這版會使用工具內已儲存的相機清單與固定通知文字，不會自動根據每條 Alarm 換相機或換文字。**

### 12-6. 做一次真正的現場驗收

1. 先按一次 UniFi 的 Test Alarm，測試規則是否能送到工具。
2. 再測一次時，至少間隔 60 秒，避免被本工具視為測試連點。
3. 用實際動作觸發：開門，或走入指定偵測範圍。
4. 等待截圖延遲和傳送時間，查看手機 LINE。
5. 到工具第 6 步按 **重新檢查**，看最近事件結果。
6. 確認進入與離開兩個方向都能拍到需要的範圍。
7. 勾選「實際觸發後，手機也收到正確的圖片」。

這些勾選是你本次人工確認的記號，不是雲端自動驗證；重新載入後不一定保留。

**完成標準：真實事件發生後，手機收到正確相機的畫面。只有 Test Alarm 成功還不算全部完成。**

## 十三、設定完成後怎麼使用？

### 平常不需要一直開著這個網頁

部署與驗收完成後，可以關閉本機工具與電腦。不要關掉現場的 UniFi 主機、相機或網路，也不要停用對應 Alarm。

### 修改相機、文字或延遲

1. 開啟原客戶資料夾的啟動檔。
2. 到「選擇攝影機」修改。
3. 按「儲存並繼續」。
4. 到「檢查與部署」重新部署。
5. 發送一次測試，再實際觸發驗收。

只按儲存，不會改到正在雲端執行的設定。

### 更換 API key 或 token

1. 在相應平台建立替代金鑰，由管理者妥善處理舊金鑰效力。
2. 到工具「連接服務」貼入新值，其他已設定金鑰留空保留。
3. 儲存並重新部署，再驗收。

不要未確認就反覆重新發行 LINE token。若金鑰疑似外洩，應先由管理者撤銷外洩權限，再安排恢復服務。

### 圖片會保存多久？

本版雲端圖片網址約在建立 24 小時後失效；R2 的實際清理可能稍晚。LINE 已下載到聊天中的圖片不會因此自動刪除。

圖片網址持有者在有效期內可讀取畫面，因此不要把網址公開。監視畫面可能包含人員與場所資訊，請只發給有權查看的人。

### 分享給下一位客戶

只分享工具產生的 `dist/unifi-line-kit.zip`。

不要自行壓縮已設定的工作資料夾：裡面的隱藏資料夾 `.private` 含金鑰。備份客戶設定時，請放在客戶授權、受保護的位置；分享程式和備份金鑰是兩回事。

## 十四、常見問題：照這個順序處理

| 看到的現象 | 先做什麼 |
|---|---|
| 找不到 Node.js／啟動檔一閃而過 | 確認已安裝 Node.js 22+，關閉舊終端後重開；無法處理時把不含金鑰的錯誤交給安裝人員 |
| 網頁打不開 | 確認終端仍在執行，用本次啟動顯示的新網址，不用上次的連接埠 |
| 要求重新開啟網頁 | 從啟動器取得新的完整網址；不要把網址中的連線憑證刪掉再首次開啟 |
| 帳戶 ID 格式錯誤 | 只貼 Cloudflare 的 32 位帳戶 ID，不貼 URL、Zone ID 或 token |
| Cloudflare 403／10000 | 核對三項編輯權限、正確帳戶範圍、token 到期及 IP 限制；不要直接擴大成所有帳戶權限 |
| `/workers/scripts` 權限錯誤 | 確認是 Workers 指令碼，不是 Workers 代理程式設定 |
| `/workers/subdomain` 權限錯誤 | 先檢查 Workers 指令碼權限與帳戶範圍；403 不代表子網域不存在 |
| R2 尚未開通 | 到 Cloudflare R2 完成客戶同意的啟用程序；不用自行亂建 bucket |
| 相機清單空白／401／403／404 | 核對完整 Console ID、API key 存取權、主機在線及 Protect 版本 |
| 部署部分成功後失敗 | 保留同一資料夾與 `.private`，修正錯誤再檢查；不要刪除雲端資源或重新建帳戶 |
| 同名資源存在／所有權紀錄缺失 | 停下來請安裝人員核對資源歸屬，不要強制覆蓋 |
| LINE Verify 失敗 | 確認已部署、URL 結尾是 `/line`，且 Channel secret 來自同一個 channel |
| Verify 成功，但配對不成功 | 開啟 Use webhook、加對機器人好友、私人對話傳完整新碼、確認未過期 |
| 配對碼已到期 | 重新按開始配對，用最新的一組；不要重用舊碼 |
| 只有歡迎訊息 | 那是官方帳號自動回覆，不是本工具已連接；查看工具的配對狀態 |
| LINE 已接受，但手機沒有通知 | 檢查是否封鎖、是否配對到另一個人或另一個 Bot，並核對官方帳號用量與錯誤 |
| LINE 401 | 檢查 access token 是否有效、是否填到正確欄位，更新後重新部署 |
| LINE 429 | 到 LINE 後台核對頻率限制或訊息額度，先不要反覆測試 |
| 圖片檢查失敗 | 先看手機是否已收到；再次測試會再發送。請安裝人員核對 R2 圖片網址與相機圖片大小 |
| SNAPSHOT_SIZE／圖片大小不適合 | 本版要求圖片約 1KB–1MB，且不自動縮圖；請安裝人員評估相機輸出或程式調整 |
| 第一次 Test Alarm 成功，後面沒收到 | 至少等 60 秒，查最近事件是否被標示重複；不要把防連點當作所有真實事件都只能每分鐘一次 |
| 工具測試成功，但真實事件沒收到 | 回 UniFi 重新開啟已存 Alarm，核對啟用狀態、時段、來源、條件、POST 與完整 URL |
| 畫面拍不到人 | 調整相機角度、截圖相機及延遲；5 秒不是每個動線都適用 |
| 重複收到很多通知 | 核對是否有多條 Alarm／重複 Webhook，以及動作偵測是否過於頻繁；先別重複建立新的規則 |

需要協助時，提供「哪個步驟、錯誤文字、發生時間、是否已在手機收到」即可。提供截圖前遮住金鑰、配對碼、完整 Webhook URL 與不必要的監視畫面。

## 十五、交付前最後確認

- [ ] 帳戶與金鑰屬於正確客戶。
- [ ] 相機名稱、數量及排列順序正確。
- [ ] 通知文字與截圖時間符合現場需求。
- [ ] 部署完成，LINE 顯示已連接。
- [ ] 手機收到手動測試的每張圖片。
- [ ] UniFi Alarm 已儲存、重新打開核對，並已啟用。
- [ ] 實際事件觸發後，手機也收到正確通知。
- [ ] 原客戶資料夾與金鑰已妥善保管。
- [ ] 客戶知道免費額度有上限、通知不保證每次必達。
- [ ] 分享出去的是乾淨的 ZIP，不是含 `.private` 的工作資料夾。

## 進階備用：只有啟動捷徑無法使用時才看

在套件資料夾開啟終端，執行：

```sh
npm ci
npm run ui
```

其他備用指令：

| 指令 | 用途 |
|---|---|
| `npm run folder` | 打開本客戶的私密資料夾，注意不要分享內容 |
| `npm run doctor` | 檢查連線，不發 LINE |
| `npm run deploy` | 部署或更新雲端服務 |
| `npm run pair` | 開始 LINE 配對 |
| `npm run status` | 查看配對及最近 Alarm 結果 |
| `npm run test:live` | 真正發送一則測試通知 |
| `npm run webhook -- line` | 顯示 LINE 的網址與有效配對碼 |
| `npm run webhook -- unifi` | 顯示 UniFi 的完整 Webhook，含金鑰 |
| `npm run checklist` | 查看驗收清單 |
| `npm run share` | 重新產生可分享 ZIP |

一般使用者按網頁上的按鈕即可，不必同時操作終端和網頁。也不要在同一份客戶資料夾同時啟動多個設定工具。

---

本指南依目前套件實作及上述官方資料整理。UniFi／LINE 設定技能中的「分客戶隔離、保護金鑰、分階段測試、儲存後再核對」已納入操作流程；圖片保存與按鈕行為則以本套件 1.2 版為準。
